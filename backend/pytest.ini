[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
markers =
    realdata: tests that run against the gitignored samples/ dataset (skipped if absent)
    integration: tests that need external services (e.g. a Mailpit SMTP catcher); skipped if unreachable
